services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ledgr
      POSTGRES_PASSWORD: ledgr
      POSTGRES_DB: ledgr
    ports:
      - "5432:5432"
    volumes:
      - ledgr_pgdata:/var/lib/postgresql/data

volumes:
  ledgr_pgdata:
